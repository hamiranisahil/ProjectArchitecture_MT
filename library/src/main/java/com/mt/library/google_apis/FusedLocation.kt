package com.mt.library.google_apis

class FusedLocation {

    private var TAG = FusedLocation().javaClass.simpleName

    companion object {
        const val INTERVAL = 1000 * 10
        const val FASTEST_INTERVAL = 1000 * 10
    }


}